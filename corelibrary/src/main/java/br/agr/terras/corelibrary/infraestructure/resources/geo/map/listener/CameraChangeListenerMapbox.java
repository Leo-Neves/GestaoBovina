package br.agr.terras.corelibrary.infraestructure.resources.geo.map.listener;

/**
 * Created by leo on 04/08/16.
 */
public class CameraChangeListenerMapbox /*implements MapboxMap.OnCameraChangeListener */{
    /*private MapboxMap.OnCameraChangeListener clusterListener;
    private MapboxMap.OnCameraChangeListener customListener;

    public CameraChangeListenerMapbox(MapboxMap.OnCameraChangeListener clusterListener){
        this.clusterListener = clusterListener;
    }

    public void setClusterListener(MapboxMap.OnCameraChangeListener clusterListener){
        this.clusterListener = clusterListener;
    }

    public void setCustomListener(MapboxMap.OnCameraChangeListener customListener){
        this.customListener = customListener;
    }

    @Override
    public void onCameraChange(CameraPosition cameraPosition) {
        if (clusterListener!=null)
            clusterListener.onCameraChange(cameraPosition);
        if (customListener!=null)
            customListener.onCameraChange(cameraPosition);
    }*/
}
